interface NormalizedLocation {
  id: string;
  timestamp: string;
  ipAddress: string;
  city: string;
  country: string;
  latitude: number;
  longitude: number;
}

export default NormalizedLocation;
